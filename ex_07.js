const mad = Vue.createMad({
   data() {
    return {
        mouseX: 0,
        mouseY: 0, 
        imageX: window.innerWidth /2,
        imageY: window.innerHeight /2
    }
   },
   methods: {
    updateMousePosition(event) {}
   ,
   moveImage() {}
    
   },
   mounted() {}
}) 
 AudioParamMap.mount('#app')
